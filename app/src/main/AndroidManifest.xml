<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ng.ui">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".testchumo.TestActivity">
        </activity>

        <activity android:name=".testchumo.testjava">

        </activity>

        <activity android:name=".aty.CdActivity"
            >

        </activity>

        <activity android:name=".aty.SvActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".aty.PtlActivity">

        </activity>

        <activity android:name=".aty.WvActivity" />
        <activity android:name=".aty.CtbActivity" />
        <activity android:name=".aty.EcgActivity" />
        <activity android:name=".aty.PlActivity" />

        <activity android:name=".aty.AiActivity"/>
    </application>

</manifest>